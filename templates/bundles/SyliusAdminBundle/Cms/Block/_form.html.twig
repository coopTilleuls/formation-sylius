<div class="ui two columns grid">
    <div class="column">
        {{ form_row(form.name) }}
        {{ form_row(form.enabled) }}
        {{ form_row(form.type) }}
    </div>
    <div class="column">
        {{ form_row(form.content) }}
    </div>
</div>
